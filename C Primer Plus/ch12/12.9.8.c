#include <stdio.h>
int * make_array(int elem, int val);
void show_array(int elem, int val);
int main(void)
{
    int * pa;
    int size;
    int value;

    printf("Enter the initiallization value")
}